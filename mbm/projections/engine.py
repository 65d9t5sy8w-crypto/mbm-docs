from mbm.domain.models import ProjectionResult


class ProjectionEngine:
    def project(self) -> ProjectionResult:
        raise NotImplementedError("Projection implementation begins in the next drop.")
